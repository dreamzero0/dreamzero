# @package _global_

# G2 absolute-action training (rich action space / canonical subtask data).
# Difference vs. g2_relative.yaml: relative_action=false, so the model learns
# to predict ABSOLUTE joint targets directly (no current-state delta
# conversion).  Deployment must use the client with --g2-arm-mode absolute.
#
# Set the data root via env:
#   G2_DATA_ROOT=/data/training_data/teleop/g2/g2_tasks_g1_g7_joint_gear_subtask_v4_stratified_canonical/train

defaults:
  - dreamzero/base_48_wan_fine_aug_relative
  - _self_

max_state_dim: 64
use_global_metadata: false
relative_action: false
relative_action_per_horizon: false
relative_action_keys: []
max_chunk_size: 5
dataset_shard_sampling_rate: 0.1
active_hold_index_path: ${g2_data_root}/meta/g2_active_hold_windows.json
active_window_ratio: 0.8
mixture_dataset_cls: groot.vla.data.dataset.lerobot_sharded.ShardedLeRobotMixtureDataset.from_mixture_spec
single_dataset_cls: groot.vla.data.dataset.lerobot_sharded.ShardedLeRobotSubLangSingleActionChunkDatasetDROID

g2_data_root: ???

train_dataset:
  _target_: ${mixture_dataset_cls}
  _convert_: object
  mixture_spec:
    - dataset_path:
        g2:
          - ${g2_data_root}
      dataset_weight: 1.0
      distribute_weights: true

  dataset_class: ${single_dataset_cls}
  all_modality_configs: ${modality_configs}
  all_transforms: ${transforms}
  metadata_versions: ${metadata_versions}
  fps: ${fps}
  dataset_kwargs:
    video_backend: decord
    use_global_metadata: ${use_global_metadata}
    max_chunk_size: ${max_chunk_size}
    relative_action: ${relative_action}
    relative_action_keys: ${relative_action_keys}
    relative_action_per_horizon: ${relative_action_per_horizon}
  mixture_kwargs:
    training: true
    balance_dataset_weights: false
    seed: 42
    shard_sampling_rate: ${dataset_shard_sampling_rate}
    active_hold_index_path: ${active_hold_index_path}
    active_window_ratio: ${active_window_ratio}
